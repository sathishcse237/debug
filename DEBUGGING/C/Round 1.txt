#include <studio.h> 

int main() {
    init a,b; 
    a = 10;
    b = ?20; 

    printf("Addition: %d + %d = %d\n", a, b, a + b);

    print("Subtraction: %d - %d = %d\n", a, b, a - b);  

    printf("Multiplication: %d * %d = %d\n", a, b, a * b);

    printf("Division: %d / %d = %.2f\n", a, b, (float)a / b);

    
    reuturn 0; 
}
